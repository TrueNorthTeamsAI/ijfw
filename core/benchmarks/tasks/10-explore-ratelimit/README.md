# Task 10 — Explore: locate rate-limiter

The repo in `repo/` contains multiple Python files. One of them implements a
token-bucket rate limiter. Identify the file and report its relative path
as a single line: `PATH: <relative/path.py>`. Do not modify any files.
